$('.comment-index').append("<%= escape_javascript(render partial: 'comments/comment', locals: {comment: @comment})%>");
$('.comment-form')[0].reset();
$('.update-comment-create').html("コメント数：<%= @book.comments.count %>件");